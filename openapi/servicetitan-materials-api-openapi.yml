openapi: 3.2.0
info:
  title: ServiceTitan Pricebook Materials API
  description: 'The Pricebook API provides CRUD over the technician-facing flat-rate pricebook — categories,

    services, materials, equipment, discounts and fees, images, and bulk operations. Tenant-scoped;

    OAuth 2.0 + App Key.'
  version: 2.0.0
  contact:
    name: ServiceTitan Developer Support
    url: https://developer.servicetitan.io/
    email: integrations@servicetitan.com
  license:
    name: ServiceTitan Terms of Service
    url: https://www.servicetitan.com/legal/terms-of-service
servers:
- url: https://api.servicetitan.io/pricebook/v2/{tenant}
  description: Production
  variables:
    tenant:
      default: '0000000'
- url: https://api-integration.servicetitan.io/pricebook/v2/{tenant}
  description: Integration (Sandbox)
  variables:
    tenant:
      default: '0000000'
security:
- OAuth2: []
  AppKey: []
tags:
- name: Materials
paths:
  /materials:
    get:
      summary: List Materials
      operationId: listMaterials
      tags:
      - Materials
      parameters:
      - $ref: '#/components/parameters/Page'
      - $ref: '#/components/parameters/PageSize'
      - $ref: '#/components/parameters/ModifiedOnOrAfter'
      responses:
        '200':
          description: Paginated materials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaterialPagedResponse'
    post:
      summary: Create Material
      operationId: createMaterial
      tags:
      - Materials
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MaterialCreateRequest'
      responses:
        '200':
          description: Created material
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Material'
  /materials/{id}:
    get:
      summary: Get Material
      operationId: getMaterial
      tags:
      - Materials
      parameters:
      - $ref: '#/components/parameters/Id'
      responses:
        '200':
          description: Material
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Material'
    patch:
      summary: Update Material
      operationId: updateMaterial
      tags:
      - Materials
      parameters:
      - $ref: '#/components/parameters/Id'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Material'
      responses:
        '200':
          description: Updated material
components:
  schemas:
    MaterialPagedResponse:
      type: object
      properties:
        page:
          type: integer
        pageSize:
          type: integer
        hasMore:
          type: boolean
        data:
          type: array
          items:
            $ref: '#/components/schemas/Material'
    MaterialCreateRequest:
      type: object
      required:
      - code
      - displayName
      - cost
      - price
      properties:
        code:
          type: string
        displayName:
          type: string
        description:
          type: string
        cost:
          type: number
        price:
          type: number
        active:
          type: boolean
        taxable:
          type: boolean
    Material:
      type: object
      properties:
        id:
          type: integer
          format: int64
        code:
          type: string
        displayName:
          type: string
        description:
          type: string
        cost:
          type: number
        price:
          type: number
        memberPrice:
          type: number
          nullable: true
        addOnPrice:
          type: number
          nullable: true
        addOnMemberPrice:
          type: number
          nullable: true
        active:
          type: boolean
        taxable:
          type: boolean
        account:
          type: string
          nullable: true
        categories:
          type: array
          items:
            type: integer
        vendorParts:
          type: array
          items:
            type: object
            properties:
              vendorId:
                type: integer
              vendorPartNumber:
                type: string
              cost:
                type: number
        primaryVendor:
          type: object
          properties:
            vendorId:
              type: integer
            vendorPartNumber:
              type: string
        modifiedOn:
          type: string
          format: date-time
  parameters:
    PageSize:
      name: pageSize
      in: query
      schema:
        type: integer
        default: 50
        maximum: 500
    Page:
      name: page
      in: query
      schema:
        type: integer
        default: 1
    Id:
      name: id
      in: path
      required: true
      schema:
        type: integer
        format: int64
    ModifiedOnOrAfter:
      name: modifiedOnOrAfter
      in: query
      schema:
        type: string
        format: date-time
  securitySchemes:
    OAuth2:
      type: oauth2
      flows:
        clientCredentials:
          tokenUrl: https://auth.servicetitan.io/connect/token
          scopes: {}
    AppKey:
      type: apiKey
      in: header
      name: ST-App-Key
